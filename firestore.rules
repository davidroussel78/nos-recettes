rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Seuls les utilisateurs authentifiés peuvent lire et écrire les recettes
    match /recipes/{recipeId} {
      allow read, write: if request.auth != null;
    }

  }
}
